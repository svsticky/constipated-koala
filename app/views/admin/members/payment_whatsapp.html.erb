Hey! <%= current_user.credentials.first_name %> van Sticky hier :)

De volgende activiteiten staan open:
<% @participants.each do |p| %>
- <%= p.activity.name %> - <%= number_to_currency(p.currency, unit: '€') %>
<% end %>

Dit geeft een totaal van *<%= number_to_currency(@participants.map(&:currency).sum, unit: '€') %>*

Kan je dit overmaken naar *NL61INGB0002877106* onder vermelding van _Openstaand <%= @member.name %>_?
