!!!
%html
  %head
    %title= 'Constipated koala'
    = stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true
    = javascript_include_tag 'application', 'data-turbolinks-track' => true
    = javascript_include_tag :defaults
    = csrf_meta_tags
  %body#app
    .view-container
      .page-signin
        .signin-header
          .container.text-center
            %section.logo
              %a.ng-binding{href: '/'} Constipated koala
        .signin-body
          .container{style: 'margin-top: 100px;'}
            .form-container
              = form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f|
                = devise_error_messages!
                
                %fieldset
                  .form-group.lead Om een nieuw wachtwoord in te stellen, krijg je een e-mail met daarin een link om je wachtwoord te wijzigen!
                  .form-group
                    .input-group.input-group-lg
                      %span.input-group-addon
                        %span.glyphicon.glyphicon-envelope
                      = f.email_field :email, autofocus: true, placeholder: 'E-mailadres', :class => 'form-control'
            
                  .form-group
                    = f.submit 'Versturen', :class => 'btn btn-primary btn-lg btn-block'