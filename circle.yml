general:
  branches:
    ignore:
      - /doc\/.*/
      
machine:
  timezone:
    Europe/Amsterdam

  environment:
    RAILS_ENV: staging

dependencies:
  bundler:
    without: [production, development]
    
dependencies:
  cache_directories:
    - "vendor/bundle" 
