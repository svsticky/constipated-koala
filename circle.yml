general:
  branches:
    ignore:
      - /doc\/.*/

machine:
  timezone:
    Europe/Amsterdam

  environment:
    RAILS_ENV: test

dependencies:
  bundler:
    without: [production, staging, development]

dependencies:
  cache_directories:
    - "vendor/bundle"
