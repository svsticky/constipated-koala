# Development
# Running on local machine using sqlite
development:
  adapter: mysql2
  database: koala-development
  hostname: localhost
  user: <%= ENV['DATABASE_USER'] %>
  pass: <%= ENV['DATABASE_PASSWORD'] %>
  pool: 5
  timeout: 5000
  encoding: utf8

# test
# Version before release on circle
test:
  adapter: mysql2
  database: koala
  hostname: localhost
  username:
  password:
  pool: 5
  timeout: 5000
  encoding: utf8

# Production
# Version for public use running on svsticky.nl
production:
  adapter: mysql2
  database: koala
  hostname: localhost
  username: <%= ENV['DATABASE_USER'] %>
  password: <%= ENV['DATABASE_PASSWORD'] %>
  socket: /var/run/mysqld/mysqld.sock
  pool: 5
  timeout: 5000
  encoding: utf8
